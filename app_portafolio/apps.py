from django.apps import AppConfig


class AppPortafolioConfig(AppConfig): # Cambia el nombre de la clase
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'app_portafolio' # Cambia el nombre de la aplicación
